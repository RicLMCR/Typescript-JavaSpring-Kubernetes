import { render, screen } from "@testing-library/react";
import List from "./List";

// Check for <h2> with 'Sample List'
// Check for object with list inc 'steve smith'
// check for object with random name sourced from DB

// test("renders learn react link", () => {
//   render(<List />);
//   const linkElement = screen.getByText(/Steve Smith/i);
//   expect(linkElement).toBeInTheDocument();
// });
